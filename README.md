# 🎵 Time Travel Spotify Playlist

This Python project allows you to **"travel back in time"** and automatically create a Spotify **TOP 10** playlist containing the **Billboard Hot 100 tracks** from a specific date (YYYY-MM-DD).  
Just enter a date, the app will fetch historical Billboard chart data and build a playlist in your Spotify account.

---

## 🚀 Features

✅ Spotify OAuth login (opens browser, user authorizes, paste redirect URL back)  
✅ Fetch Billboard Hot 100 chart by date  
✅ Search each track on Spotify API and collect URIs  
✅ Automatically create a **new playlist** (or reuse existing one if already created before)  
✅ Add all found tracks to the playlist  
✅ Data persistence using CSV  
✅ CLI interface (no GUI)



